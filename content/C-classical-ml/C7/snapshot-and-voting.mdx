---
title: "Snapshot ensembles and voting"
pillar: C
subId: C7
lessonId: snapshot-and-voting
order: 3
estimatedMinutes: 22
goal: "Average models cheaply via snapshots, hard/soft voting, and rank averaging."
prerequisites: ["ensemble-theory"]
---

Stacking is powerful but expensive. **Snapshot ensembles** and **voting**
are cheap shortcuts that capture much of the benefit: get multiple models
nearly for free, then combine their predictions with simple averaging or
voting.

## Snapshot ensembles

Train one model but save many checkpoints during training. With a cyclic
learning-rate schedule (cosine annealing with restarts is standard), each
restart converges to a different local minimum. Saving the model at each
minimum produces a sequence of **diverse** snapshots, each one a usable
model. Average their predictions at inference<FN n="1" />.

The catch versus a true ensemble of independently trained models:
snapshots from one training run are more correlated than fully independent
trainings, so the variance reduction is smaller. But the *cost* is one
training run instead of many.

Used heavily in deep learning competitions where each training run is
expensive.

## Hard voting

For classification, **hard voting** picks the class that the majority of
models predict<FN n="2" />:

$$
\hat y = \arg\max_c \sum_m \mathbf{1}[\hat y_m = c].
$$

Simple, robust, no probability calibration needed. Tied votes can be broken
randomly or by model rank.

## Soft voting

If each base model produces calibrated probabilities $p_m(y \mid x)$,
average them and take the argmax:

$$
\hat y = \arg\max_c \frac{1}{M}\sum_m p_m(c \mid x).
$$

Soft voting uses more information (the probabilities, not just the argmax)
and usually outperforms hard voting when models are well-calibrated.

**Critical caveat**: averaging miscalibrated probabilities is worse than
hard voting. Calibrate first (Platt or isotonic, covered in C10), then
average.

## Rank averaging

When models output scores on different scales (one returns probabilities,
another returns SVM decision values), direct averaging is meaningless.
**Rank averaging** converts each model's scores to ranks, averages the ranks
across models, and uses the average rank as the final score:

$$
\text{rank-avg}(x_i) = \frac{1}{M}\sum_m \text{rank}_m(x_i).
$$

Scale-invariant and robust to outlier scores. Common in classification and
ranking competitions where leaderboard scores are AUC or NDCG.

## Weighted voting

If you know some models are better than others, weight the votes by
held-out performance:

$$
\hat y = \arg\max_c \sum_m w_m\, p_m(c \mid x), \quad w_m \propto \text{val accuracy}_m.
$$

A poor man's stacking: cheaper, and surprisingly hard to beat with a
trained meta-learner unless you have a lot of validation data.

## When to use each

- **Snapshot ensemble**: one expensive training run, want some ensembling
  benefit. Deep learning training.
- **Hard voting**: classifiers with uncalibrated outputs, or robustness
  matters more than accuracy.
- **Soft voting**: calibrated probabilistic classifiers, want to combine
  multiple uncertainty estimates.
- **Rank averaging**: heterogeneous model outputs on different scales,
  ranking metrics.
- **Stacking (previous lesson)**: have enough validation data and want
  squeeze the last bit of accuracy.

```mermaid
graph LR
  A["cyclic learning rate<br/>cosine restarts"] --> B["each restart settles<br/>in a new minimum"]
  B --> C["save a snapshot<br/>at each minimum"]
  C --> D["average or vote<br/>across snapshots"]
```

## Check it on arrays

```python
import numpy as np

rng = np.random.default_rng(0)

# Synthetic problem with 3 base models of different qualities
n_test = 500
y_true = rng.integers(0, 2, size=n_test)

# Each base model is the truth corrupted with model-specific noise
def make_model_preds(y_true, accuracy, rng):
    p_correct = accuracy
    p = np.where(y_true == 1,
                 rng.uniform(0.55, 0.95, size=n_test),
                 rng.uniform(0.05, 0.45, size=n_test))
    # Flip some labels
    flip = rng.random(n_test) > p_correct
    pred = (p > 0.5).astype(int)
    p_used = np.where(flip, 1 - p, p)
    return p_used, np.where(flip, 1 - pred, pred)

p1, h1 = make_model_preds(y_true, 0.78, np.random.default_rng(1))
p2, h2 = make_model_preds(y_true, 0.74, np.random.default_rng(2))
p3, h3 = make_model_preds(y_true, 0.70, np.random.default_rng(3))

print(f"single-model accuracies: {np.mean(h1 == y_true):.3f}, "
      f"{np.mean(h2 == y_true):.3f}, {np.mean(h3 == y_true):.3f}")

# Hard voting
hard_vote = (h1 + h2 + h3 >= 2).astype(int)
print(f"hard vote accuracy:  {np.mean(hard_vote == y_true):.3f}")

# Soft voting
soft_vote = ((p1 + p2 + p3) / 3 > 0.5).astype(int)
print(f"soft vote accuracy:  {np.mean(soft_vote == y_true):.3f}")

# Rank averaging
def to_rank(p): return p.argsort().argsort()
rank_avg = (to_rank(p1) + to_rank(p2) + to_rank(p3)) / (3 * n_test)
rank_pred = (rank_avg > 0.5).astype(int)
print(f"rank-average accuracy: {np.mean(rank_pred == y_true):.3f}")

# Weighted voting by individual accuracy
acc = np.array([np.mean(h1 == y_true), np.mean(h2 == y_true), np.mean(h3 == y_true)])
w = acc / acc.sum()
weighted = (w[0] * p1 + w[1] * p2 + w[2] * p3 > 0.5).astype(int)
print(f"weighted soft vote:   {np.mean(weighted == y_true):.3f}")
```

Both hard and soft voting beat the best single model when the models are
roughly independent. Rank averaging gives similar results without needing
calibrated probabilities.

## Exercises

**Exercise 1.** Three calibrated binary classifiers output, for one input, the
probabilities of class $1$: $p_1 = 0.8$, $p_2 = 0.45$, $p_3 = 0.6$. Give the hard
vote and the soft vote, and explain why they disagree.

<details>
<summary>Solution</summary>

**Step 1.** Hard vote: threshold each at $0.5$. Model 1 predicts class $1$
($0.8 > 0.5$), model 2 predicts class $0$ ($0.45 < 0.5$), model 3 predicts class
$1$ ($0.6 > 0.5$). The majority is $2$ to $1$ for class $1$.

**Step 2.** Soft vote: average the probabilities,
$\frac{0.8 + 0.45 + 0.6}{3} = \frac{1.85}{3} \approx 0.617$, then threshold:
$0.617 > 0.5$, so class $1$.

**Step 3.** Here they agree on the label, but the soft vote also carries the
confidence $0.617$, whereas the hard vote discards it. They can disagree when the
majority is confident-but-wrong on the count: if probabilities were
$(0.51, 0.51, 0.05)$, hard voting says class $1$ ($2$ to $1$) while soft voting
averages to $0.357$ and says class $0$. Soft voting lets one strongly-opposed
model overrule two weakly-supporting ones.

</details>

**Exercise 2.** The Condorcet jury theorem says that for $M$ independent voters
each correct with probability $p > 1/2$, majority-vote accuracy increases with
$M$ and tends to $1$. Explain why the independence assumption is exactly the
"correlated errors" failure mode from the ensemble-theory lesson, and what
happens to the guarantee when the voters are perfectly correlated.

<details>
<summary>Solution</summary>

**Step 1.** The theorem's power comes from averaging out independent mistakes:
with independent voters, the probability that more than half are simultaneously
wrong shrinks as $M$ grows, by the law of large numbers applied to the count of
correct votes.

**Step 2.** If the voters are perfectly correlated, every voter is wrong exactly
when the others are wrong, so the majority is wrong with the same probability
$1 - p$ regardless of $M$. The count of correct votes is either $M$ or $0$, never
a partial split, so adding voters does nothing.

**Step 3.** This is the correlated-errors failure mode: ensembling buys accuracy
only to the extent the base models err on different inputs. Snapshots from one
training run are more correlated than independent trainings, which is precisely
why their variance reduction is smaller than a true ensemble's.

</details>

**Exercise 3 (code).** Simulate $M$ independent classifiers each correct with
probability $p = 0.65$ and measure majority-vote accuracy for
$M \in \{1, 3, 5, 11, 25\}$. Confirm accuracy rises with $M$ and that $25$ voters
beat a single voter by a wide margin.

<details>
<summary>Solution</summary>

```python
import numpy as np

rng = np.random.default_rng(0)

n = 200_000      # trials
p = 0.65         # per-voter accuracy

def majority_accuracy(M, p, rng):
    correct = rng.random((n, M)) < p          # each voter correct w.p. p
    return (correct.sum(axis=1) > (M / 2)).mean()

for M in [1, 3, 5, 11, 25]:
    print(f"M={M:2d}: majority-vote accuracy = {majority_accuracy(M, p, rng):.4f}")

acc1 = majority_accuracy(1, p, rng)
acc25 = majority_accuracy(25, p, rng)
assert acc25 > acc1      # more voters help
assert acc25 > 0.90      # 25 voters at p=0.65 clear 90%
```

A single voter sits at $0.65$, but the majority of $25$ independent voters reaches
about $0.94$: independent errors cancel in the count, exactly the Condorcet
mechanism. The same simulation with perfectly correlated voters would stay flat
at $0.65$ for every $M$.

</details>

The next lesson hands the model search to an automatic system: AutoML.

<Footnotes>
  <FNItem n="1">**Hansen, L. K., & Salamon, P.** (1990). "Neural network ensembles". *IEEE TPAMI*, 12(10), 993-1001. Averaging multiple trained networks improves accuracy over any single one ([doi:10.1109/34.58871](https://doi.org/10.1109/34.58871)).</FNItem>
  <FNItem n="2">**Breiman, L.** (1996). "Bagging predictors". *Machine Learning*, 24(2), 123-140. Majority voting over aggregated predictors as the classification analogue of averaging ([doi:10.1007/BF00058655](https://doi.org/10.1007/BF00058655)).</FNItem>
</Footnotes>
