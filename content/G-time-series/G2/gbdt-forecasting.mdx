---
title: "Gradient-boosted trees for forecasting"
pillar: G
subId: G2
lessonId: gbdt-forecasting
order: 2
estimatedMinutes: 34
goal: "Forecast with gradient-boosted trees on engineered features, and contrast the recursive and direct multi-step strategies."
prerequisites: ["ts-feature-engineering", "C3/xgboost-lightgbm"]
---

Gradient-boosted decision trees (GBDT) win a large share of forecasting
competitions when the data is tabular and rich in covariates<FN n="1" />. They take the
feature table from the previous lesson and learn nonlinear interactions between
lags, calendar effects, and external regressors that linear ARIMA cannot. The new
question they raise is how to forecast more than one step ahead, because a tree
predicts a single number, not a trajectory.

## Why trees fit forecasting

A boosted-tree ensemble approximates the target as a sum of regression trees,
$\hat{y} = \sum_b f_b(\mathbf{x})$, each $f_b$ fit to the gradient of the loss left
by its predecessors. The gradient-boosting lesson covers the mechanics. For
time series the appeal is specific:

- Trees split on thresholds, so they model regime changes (a holiday flag, a
  promotion) without manual interaction terms.
- They ingest many heterogeneous features (lags, rolling stats, prices, weather)
  on different scales without normalization.
- They are robust to irrelevant features, so over-generating lag candidates is
  cheap.

What trees cannot do is **extrapolate**. A tree's prediction is bounded by the leaf
values seen in training, so a series with an unbounded trend must be differenced or
detrended first; feed the model stationary targets (for example, the change
$y_t - y_{t-1}$ or a ratio) and add the trend back afterward.

## Recursive multi-step forecasting

The first strategy trains a single one-step model $\hat{y}_{t+1} = g(\mathbf{x}_t)$
and applies it repeatedly, feeding each prediction back in as the lag input for the
next step:

$$
\hat{y}_{t+2} = g(\mathbf{x}_{t+1}), \quad \text{where } \mathbf{x}_{t+1} \text{ contains } \hat{y}_{t+1}.
$$

- **Advantage:** one model, and it uses the most recent lag at every step.
- **Disadvantage:** errors compound. A mistake at step 1 becomes a corrupted input
  at step 2, and the model was trained on *clean* lags, never on its own noisy
  predictions, so the train and inference distributions diverge as the horizon
  grows.

## Direct multi-step forecasting

The second strategy trains a **separate model per horizon**. Model $g_h$ predicts
$y_{t+h}$ directly from $\mathbf{x}_t$:

$$
\hat{y}_{t+h} = g_h(\mathbf{x}_t), \qquad h = 1, \dots, H.
$$

- **Advantage:** no error feedback; each model optimizes its own horizon, and
  inference sees the same feature distribution as training.
- **Disadvantage:** $H$ models to train and store, and each ignores the others, so
  the forecast trajectory can be jagged across horizons.

A middle path, **DirRec**, trains one model per horizon but lets each use the
previous horizons' predictions, combining the strengths. In practice, recursive is
the default for long horizons with cheap retraining; direct wins when $H$ is small
and accuracy at each specific lead time matters.

```mermaid
graph LR
  T["time series"] --> F["build features:<br/>lags, rolling stats, calendar"]
  F --> G["gradient-boosted trees"]
  G --> P["next-step forecast"]
```

## A global model across many series

When forecasting thousands of related series (every product in a catalogue), a
**global** model trains one GBDT on the pooled rows of all series, with a series
identifier or static features as columns. This shares statistical strength,
regularizes short series toward the population, and beats fitting a separate model
per series, the insight behind the winning M5-competition solutions and a now-standard
recommendation in the forecasting literature<FN n="2" />.

```python
import numpy as np

rng = np.random.default_rng(2)
n, m = 800, 12
t = np.arange(n)
y = 5*np.sin(2*np.pi*t/m) + 0.5*rng.standard_normal(n) + np.cos(2*np.pi*t/50)

def feats(y, t, i, m):
    return [y[i-1], y[i-2], y[i-m], np.sin(2*np.pi*t[i]/m), np.cos(2*np.pi*t[i]/m)]

# Minimal gradient boosting on depth-1 regression stumps (squared-error loss).
class Stump:
    def fit(self, X, g):
        best = (np.inf, 0, 0.0, g.mean(), g.mean())
        for j in range(X.shape[1]):
            order = np.argsort(X[:, j]); xs, gs = X[order, j], g[order]
            for thr in np.unique(xs)[1:]:                 # candidate split points
                left = xs < thr
                if left.sum() == 0 or left.sum() == len(gs):
                    continue
                vl, vr = gs[left].mean(), gs[~left].mean()
                sse = ((gs[left]-vl)**2).sum() + ((gs[~left]-vr)**2).sum()
                if sse < best[0]:
                    best = (sse, j, thr, vl, vr)
        _, self.j, self.thr, self.vl, self.vr = best
        return self
    def predict(self, X):
        return np.where(X[:, self.j] < self.thr, self.vl, self.vr)

def gbdt_fit(X, y, rounds=120, lr=0.1):
    pred = np.full(len(y), y.mean()); base = y.mean(); trees = []
    for _ in range(rounds):
        resid = y - pred                                   # negative gradient of MSE
        s = Stump().fit(X, resid); trees.append(s)
        pred += lr * s.predict(X)
    return base, trees

def gbdt_predict(base, trees, X, lr=0.1):
    out = np.full(len(X), base)
    for s in trees:
        out += lr * s.predict(X)
    return out

start, H = m, 12
X  = np.array([feats(y, t, i, m) for i in range(start, n-H)])
y1 = np.array([y[i+1] for i in range(start, n-H)])         # one-step target
split = len(X) - 100
base, trees = gbdt_fit(X[:split], y1[:split])

# Recursive: roll the one-step model forward H steps from the split point
hist = list(y[:start+split+1]); preds = []
for _ in range(H):
    i = len(hist)
    x = np.array([[hist[i-1], hist[i-2], hist[i-m],
                   np.sin(2*np.pi*i/m), np.cos(2*np.pi*i/m)]])
    p = gbdt_predict(base, trees, x)[0]
    preds.append(p); hist.append(p)

actual = y[start+split+1 : start+split+1+H]
rmse = np.sqrt(((np.array(preds) - actual)**2).mean())
print(f"recursive {H}-step RMSE: {rmse:.2f}  (noise std 0.5)")
```

The recursive forecast tracks the seasonal signal across the full horizon, with
error a few times the noise floor, the expected accumulation from feeding
predictions back as inputs.

## Exercises

**Exercise 1.** A horizon of $H = 12$ is forecast with the direct strategy, training
one model $g_h$ per lead time. How many models must be trained and stored? Now
suppose the recursive strategy is used instead. How many models, and how many times
is the single model evaluated to produce the full 12-step trajectory?

<details>
<summary>Solution</summary>

**Step 1.** Direct trains a separate model for each horizon $h = 1, \dots, 12$, so
it produces and stores $12$ models. Each is evaluated once, from the same fixed
feature vector $\mathbf{x}_t$.

**Step 2.** Recursive trains a single one-step model $g$. It is evaluated $H = 12$
times, once per step, with the prediction from each step folded into the lag inputs
of the next.

**Step 3.** The trade is storage against error feedback: direct keeps $12$ models
but never feeds a prediction back, so each model sees the clean training
distribution; recursive keeps $1$ model but its $12$th evaluation runs on inputs
built from $11$ previous predictions, so errors accumulate along the chain.

</details>

**Exercise 2.** Trees cannot extrapolate beyond the leaf values seen in training.
Take a strictly increasing series $y_t = t$ for $t = 0, \dots, 99$ and a lag-1 model
$\hat{y}_{t+1} = g(y_t)$. Argue that a tree trained on this range cannot forecast
$y_{101} = 101$, and explain why first-differencing the target removes the problem.

<details>
<summary>Solution</summary>

**Step 1.** A regression tree predicts a leaf value, and every leaf value is an
average of training targets. The training targets are $y_1, \dots, y_{99}$, all at
most $99$. Any prediction the tree makes is bounded above by $99$, so it can never
output $101$, no matter the input.

**Step 2.** The lag-1 input $y_{100} = 100$ also falls outside the training range of
inputs ($0$ to $98$), so it lands in the rightmost leaf, whose value is fixed at the
largest training average. The forecast saturates instead of continuing the trend.

**Step 3.** Differencing replaces the target with $d_t = y_t - y_{t-1}$. For this
series $d_t = 1$ for all $t$, a stationary, bounded target the tree predicts
exactly. The forecast is reconstructed by adding the predicted increment back:
$\hat{y}_{t+1} = y_t + \hat{d}_{t+1} = 100 + 1 = 101$. The unbounded trend lives in
the cumulative sum, not in the tree.

</details>

**Exercise 3 (code).** Demonstrate the extrapolation failure and its fix without any
tree library: a nearest-neighbor-in-training rule mimics a tree's "predict a seen
leaf value" behavior. Show that predicting the level saturates on a linear trend,
while predicting the first difference and summing it back recovers the trend. End
with asserts.

<details>
<summary>Solution</summary>

```python
import numpy as np

# A tree's prediction is some training target picked by the input region.
# Nearest-neighbor in the lag-1 input reproduces that "seen value only" behavior.
def nn_predict(x_train, y_train, x_query):
    j = np.argmin(np.abs(x_train - x_query))
    return y_train[j]

t = np.arange(100.0)
y = t.copy()                                   # strictly increasing trend

# Level model: input y[t], target y[t+1]
xin  = y[:-1]                                   # y[0..98]
yout = y[1:]                                    # y[1..99]
# Forecast y[101] from y[100]=100, which is beyond the training input range.
level_pred = nn_predict(xin, yout, 100.0)
assert level_pred <= yout.max()                # bounded by largest seen target (99)
assert level_pred < 101.0                       # cannot reach the true value

# Difference model: target d[t] = y[t]-y[t-1] = 1 everywhere
d = np.diff(y)                                  # all ones
din  = d[:-1]
dout = d[1:]
d_hat = nn_predict(din, dout, d[-1])            # predicts 1.0
fixed_pred = 100.0 + d_hat                       # add increment back to the level
assert np.isclose(fixed_pred, 101.0)
print("level (saturates):", level_pred, " differenced (recovers):", fixed_pred)
```

The level model caps at the largest training target ($99$) and cannot reach
$101$, while the differenced model predicts the constant increment and reconstructs
the trend exactly.

</details>

A single GBDT treats all series with one flat model. The next lesson handles series
that are organized into a hierarchy, where forecasts must add up consistently
across levels.

<Footnotes>
  <FNItem n="1">**Makridakis, S., Spiliotis, E., & Assimakopoulos, V.** (2020). "The M4 competition: 100,000 time series and 61 forecasting methods". *International Journal of Forecasting*, 36(1), 54-74. [doi:10.1016/j.ijforecast.2019.04.014](https://doi.org/10.1016/j.ijforecast.2019.04.014). Documents the rise of machine-learning and combination methods in large-scale forecasting.</FNItem>
  <FNItem n="2">**Hyndman, R. J., & Athanasopoulos, G.** (2021). *Forecasting: Principles and Practice* (3rd ed.). OTexts. Recursive versus direct multi-step strategies and global pooled models across related series.</FNItem>
</Footnotes>
