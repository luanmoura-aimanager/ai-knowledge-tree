---
title: "Inverse RL and adversarial imitation"
pillar: H
subId: H5
lessonId: inverse-rl
order: 2
estimatedMinutes: 36
goal: "Recover a reward that explains expert behavior with maximum-entropy IRL, and connect the result to GAIL, which matches occupancy measures with a GAN-style discriminator."
prerequisites: ["imitation-learning", "A2/entropy"]
---

Behavioral cloning copies *what* the expert does. **Inverse reinforcement learning**
(IRL) recovers *why*: the reward function the expert is optimizing. A reward
generalizes far better than a copied policy, because it explains behavior in new
states the demonstrations never visited. The catch is that the problem is badly
underdetermined, and resolving that ambiguity is the whole technical content<FN n="1" />.

## The reward-ambiguity problem

Given expert trajectories, many reward functions make them optimal. The trivial
reward $r \equiv 0$ makes *every* policy optimal, the expert included. More subtly,
adding any potential-based shaping term leaves the optimal policy unchanged. So
"find a reward under which the expert is optimal" has a huge solution set, most of
them useless. IRL needs a principle to pick one.

## Maximum-entropy IRL

The **maximum-entropy** principle resolves the ambiguity by asking for the least
committed distribution over behavior consistent with the data. Among all
trajectory distributions whose expected features match the expert's, choose the one
with maximum [entropy](A2/entropy). The solution to that constrained maximization is
an exponential-family distribution in which a trajectory's probability is set by its
return under a **linear** reward $r_\theta(s) = \theta^\top \phi(s)$:

$$
p_\theta(\tau) \propto \exp\!\Big( \sum_{t} \theta^\top \phi(s_t) \Big).
$$

Higher-reward trajectories are exponentially more likely, but nothing beyond the
feature-matching constraint is assumed, which is exactly what "maximum entropy"
buys. Fitting $\theta$ by maximum likelihood gives a clean gradient:

$$
\nabla_\theta \mathcal{L} = \underbrace{\phi_{\text{expert}}}_{\text{empirical feature avg}} - \underbrace{\mathbb{E}_{p_\theta}[\phi]}_{\text{model feature avg}}.
$$

This is the generic exponential-family gradient: match the model's expected
features to the expert's. At the optimum the two are equal, so the recovered reward
reproduces the expert's feature expectations. Computing $\mathbb{E}_{p_\theta}[\phi]$
in a full MDP needs a soft (entropy-regularized) value-iteration pass, but the
feature-matching structure is the same in every case.

## GAIL: skip the reward, match occupancy

Recovering a reward and then running RL on it is a slow two-step loop. **Generative
adversarial imitation learning** (GAIL) collapses it. The state-action distribution
a policy induces is its **occupancy measure**; imitation succeeds exactly when the
learner's occupancy matches the expert's. GAIL drives that match with a GAN<FN n="2" />:

- A **discriminator** $D$ is trained to tell expert state-action pairs from the
  learner's, outputting the probability that a pair came from the expert.
- The **policy** is trained by RL (PPO) with reward $-\log(1 - D(s,a))$, which is
  high where the discriminator thinks a pair looks expert-like.

The discriminator plays the role of the recovered reward, and the adversarial game
pushes the learner's occupancy toward the expert's. GAIL imitates directly from
demonstrations without ever materializing an explicit reward, which is why it scales
to high-dimensional control where reward recovery is hard. The same adversarial
structure links it to the [GANs](D4/gan) of generative deep learning.

```mermaid
graph LR
  Ex["expert demonstrations"] --> IR["infer the reward<br/>that explains them"]
  IR --> RL["run RL on the<br/>inferred reward"]
  RL --> Pol["policy matching<br/>the expert"]
```

## Worked check: recover a reward that reproduces expert behavior

Use a one-step (contextual) version where the expert is soft-optimal for an unknown
linear reward $\theta^\top \phi(a)$. The only thing IRL observes is the expert's
feature expectation. Recover $\theta$ by the feature-matching gradient and confirm
the induced policy reproduces both the expert's feature expectations and its action
distribution.

```python
import numpy as np
rng = np.random.default_rng(0)

K, d = 6, 3
F = rng.normal(size=(K, d))                 # feature vector phi(a) of each action
theta_true = np.array([1.5, -1.0, 0.5])     # the expert's (unknown) reward weights
def softmax(u): e = np.exp(u - u.max()); return e / e.sum()

pi_expert = softmax(F @ theta_true)         # expert is soft-optimal for the reward
f_expert = pi_expert @ F                     # expert feature expectation (all IRL sees)

# Max-ent IRL: ascend the feature-matching gradient  grad = f_expert - E_pi[phi].
theta = np.zeros(d)
for _ in range(5000):
    pi = softmax(F @ theta)
    theta += 0.5 * (f_expert - pi @ F)

pi_rec = softmax(F @ theta)
print("feature expectations matched:", np.allclose(pi_rec @ F, f_expert, atol=1e-4))
print("recovered policy matches expert:", np.allclose(pi_rec, pi_expert, atol=1e-3))
print("expert policy:   ", np.round(pi_expert, 3))
print("recovered policy:", np.round(pi_rec, 3))
```

The recovered reward weights induce a policy whose feature expectations and action
probabilities match the expert's, recovering the behavior from feature statistics
alone, the maximum-entropy resolution of the reward ambiguity.

## Exercises

**Exercise 1.** The reward-ambiguity problem says many rewards make the same
expert trajectories optimal. Show concretely that the reward $r \equiv 0$ makes
every policy optimal, and explain in one sentence why max-entropy IRL does not
return it even though it fits the "expert is optimal" constraint.

<details>
<summary>Solution</summary>

**Step 1.** Under $r \equiv 0$ every trajectory has return $\sum_t r(s_t) = 0$, so
the value of every state is $0$ under every policy. No policy can do better than any
other, so every policy (the expert's included) is optimal. The constraint "the
expert is optimal" is satisfied vacuously.

**Step 2.** Max-entropy IRL adds the feature-matching constraint: the recovered
reward must reproduce the expert's expected features $\phi_{\text{expert}}$. The zero
reward induces a uniform trajectory distribution whose feature expectation is the
unweighted average over all behavior, which does not equal $\phi_{\text{expert}}$
unless the expert is itself uniform, so the zero reward violates feature matching and
is rejected.

</details>

**Exercise 2.** Maximum-likelihood fitting of the max-ent model gives the gradient
$\nabla_\theta \mathcal{L} = \phi_{\text{expert}} - \mathbb{E}_{p_\theta}[\phi]$.
Argue from this expression what is true at a stationary point, and why the recovered
reward then reproduces the expert's feature expectations.

<details>
<summary>Solution</summary>

**Step 1.** A stationary point of the log-likelihood has zero gradient:

$$
\phi_{\text{expert}} - \mathbb{E}_{p_\theta}[\phi] = 0
\quad\Longrightarrow\quad
\mathbb{E}_{p_\theta}[\phi] = \phi_{\text{expert}}.
$$

**Step 2.** The model's expected features under the fitted reward equal the expert's
empirical feature average. Since the max-ent distribution is fully determined by the
reward weights $\theta$ acting through the features, matching the feature
expectations means the recovered reward induces behavior with the same feature
statistics as the expert, which is the sense in which IRL "explains" the
demonstrations.

**Step 3.** The log-likelihood of the exponential-family model is concave in
$\theta$, so this stationary point is the global maximum: feature matching is not
just necessary but achievable.

</details>

**Exercise 3 (code).** For a one-step soft-optimal expert with linear reward
$\theta^\top \phi(a)$, verify two facts: the feature-matching gradient
$f_{\text{expert}} - \mathbb{E}_\pi[\phi]$ vanishes at the true $\theta$, and adding a
constant to every action's reward leaves the soft-optimal policy unchanged (a face of
the reward ambiguity).

<details>
<summary>Solution</summary>

```python
import numpy as np
rng = np.random.default_rng(1)

K, d = 5, 2
F = rng.normal(size=(K, d))                 # feature vector phi(a) per action
theta_true = np.array([1.0, -0.5])
def softmax(u): e = np.exp(u - u.max()); return e / e.sum()

pi_e = softmax(F @ theta_true)              # soft-optimal expert

# Estimate the expert feature expectation from sampled demonstrations, so the
# check below is empirical, not an algebraic identity.
actions = rng.choice(K, size=200_000, p=pi_e)
f_expert = F[actions].mean(axis=0)          # sampled expert feature mean

# Fact 1: the feature-matching gradient vanishes at the true theta, i.e. the
# model expectation E_pi[phi] under theta_true matches the demonstrated features.
model_feat = softmax(F @ theta_true) @ F
grad = f_expert - model_feat
assert np.linalg.norm(grad) < 1e-2          # zero up to Monte Carlo error

# Fact 2: shifting every action's reward by a constant leaves the policy unchanged.
pi_shift = softmax(F @ theta_true + 3.7)
assert np.allclose(pi_shift, pi_e)
print("grad norm:", round(float(np.linalg.norm(grad)), 5),
      " policy invariant under shift:", np.allclose(pi_shift, pi_e))
```

The gradient vanishes exactly where the model's features match the expert's, and the
softmax cancels any constant added to all logits, the invariance that makes a bare
"expert is optimal" objective underdetermined and forces the max-entropy principle to
pin down a unique reward.

</details>

Imitation and IRL both assume an interactive environment or an online expert.
Often neither is available: there is only a fixed, logged dataset and no way to
collect more. The next lesson tackles that offline setting.

<Footnotes>
  <FNItem n="1">**Ng, A. Y., & Russell, S.** (2000). "Algorithms for inverse reinforcement learning". *ICML*. Formalizes IRL and the reward-ambiguity problem.</FNItem>
  <FNItem n="2">**Ho, J., & Ermon, S.** (2016). "Generative adversarial imitation learning (GAIL)". *NeurIPS*. [arXiv:1606.03476](https://arxiv.org/abs/1606.03476). Matches occupancy measures with a GAN-style discriminator.</FNItem>
</Footnotes>
