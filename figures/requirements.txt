# Figure generation toolchain (Path A). Run locally in a venv; NOT installed in
# CI/Vercel — generated SVGs are committed under public/figures/ and served as-is.
#   python3 -m venv figures/.venv && figures/.venv/bin/pip install -r figures/requirements.txt
#   npm run figures
matplotlib>=3.8
numpy>=1.26
scipy>=1.11
